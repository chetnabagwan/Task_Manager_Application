MANAGER_PROMPT_WLCM : |
  ---------------WELCOME MANAGER------------------

  1.View users
  2.Assign tasks 
  3.View status of assigned tasks
  4.Update status of assigned tasks
  5.Logout
  6.Exit

MANAGER_PROMPT : |

  1.View users
  2.Assign tasks 
  3.View status of assigned tasks
  4.Update status of assigned tasks
  5.Logout
  6.Exit


USER_PROMPT_WLCM : |
  ---------------WELCOME USER------------------

  1.Create new tasks
  2.Update tasks
  3.View tasks
  4.Delete tasks
  5.Logout 
  6.Exit

USER_PROMPT : |

  1.Create new tasks
  2.Update tasks
  3.View tasks
  4.Delete tasks
  5.Logout 
  6.Exit

ATTEMPTS : 3

MANAGER : "manager"

GEN_REGEX : r"^([A-Za-z0-9].\s*)"

PWD_REGEX : "^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d)(?=.*[@$!%*#?&])[A-Za-z\\d@$!#%*?&]{6,20}$"

ID_REGEX : r"^([0-9]{4}\s)"

PASSWORD_REQUIREMENTS : "A strong password should have at least one number, one uppercase character, one lowercase character,at least one special symbol and 6 to 20 characters long."

